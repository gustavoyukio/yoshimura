import { IngredientesPipe } from './ingredientes.pipe';

describe('IngredientesPipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientesPipe();
    expect(pipe).toBeTruthy();
  });
});
