import { PorcentagensPipe } from './porcentagens.pipe';

describe('PorcentagensPipe', () => {
  it('create an instance', () => {
    const pipe = new PorcentagensPipe();
    expect(pipe).toBeTruthy();
  });
});
